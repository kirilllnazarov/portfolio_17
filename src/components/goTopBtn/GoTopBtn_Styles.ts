import styled from "styled-components";

const GoTopBtn = styled.button`
	background-color: rgba(0, 0, 0, 0.2);
    border-radius: 7px;
	padding: 8px;
	position: fixed;
	right: 30px;
	bottom: 30px;
`;
export const S = {
	GoTopBtn,
};
